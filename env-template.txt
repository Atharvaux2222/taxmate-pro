# EZTaxMate Environment Variables Template
# Add these to your Replit Secrets tab (not as a .env file)

# Required Secrets (add these in Replit Secrets):
SESSION_SECRET=your_session_secret_here
GEMINI_API_KEY=your_gemini_api_key_here

# Automatic Replit Environment Variables (already available):
REPL_ID=auto_generated_by_replit
REPLIT_DOMAINS=auto_generated_by_replit
DATABASE_URL=auto_generated_by_replit
PGHOST=auto_generated_by_replit
PGPORT=auto_generated_by_replit
PGUSER=auto_generated_by_replit
PGPASSWORD=auto_generated_by_replit
PGDATABASE=auto_generated_by_replit

# Application Settings (set automatically):
NODE_ENV=development
PORT=5000
ISSUER_URL=https://replit.com/oidc

# How to use:
# 1. Go to Secrets tab in Replit sidebar
# 2. Add SESSION_SECRET and GEMINI_API_KEY
# 3. Restart your application
# 4. All other variables are set automatically by Replit